services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: keyboard
      POSTGRES_PASSWORD: keyboard
      POSTGRES_DB: keyboard
    ports:
      - '5433:5432'
    volumes:
      - keyboard_db_data:/var/lib/postgresql/data

volumes:
  keyboard_db_data:
