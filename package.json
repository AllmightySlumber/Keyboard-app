{
  "name": "keyboard-typing-app",
  "private": true,
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev:app": "npm run dev --workspace=app",
    "dev:server": "npm run dev --workspace=server"
  }
}
